## Teamvoy test (Pokedex);

Using: React, Redux

In the project directory, run: npm install -> nmp start;

In app: 
  - click on pokemon to open full info;
  - click on full info to close;
  - sort don`t work yet;